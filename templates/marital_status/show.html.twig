{% extends 'base.html.twig' %}

{% block title %}MaritalStatus
{% endblock %}

{% block body %}
	<h1>MaritalStatus</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ marital_status.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ marital_status.name }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_marital_status_index') }}">
		<button class="btn btn-secondary">back to list</button>
	</a>

	<a href="{{ path('app_marital_status_edit', {'id': marital_status.id}) }}">
		<button class="btn btn-primary">
			edit
		</button>
	</a>

	{{ include('marital_status/_delete_form.html.twig') }}
{% endblock %}
