<form method="post" action="{{ path('app_coverage_delete', {'id': coverage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coverage.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
