{% extends 'base.html.twig' %}

{% block title %}PolicyHolder
{% endblock %}

{% block body %}
	<h1>PolicyHolder</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ policy_holder.id }}</td>
			</tr>
			<tr>
				<th>FirstName</th>
				<td>{{ policy_holder.firstName }}</td>
			</tr>
			<tr>
				<th>LastName</th>
				<td>{{ policy_holder.lastName }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_policy_holder_index') }}">
		<button class="btn btn-secondary">back to list</button>
	</a>

	<a href="{{ path('app_policy_holder_edit', {'id': policy_holder.id}) }}">
		<button class="btn btn-primary">
			edit
		</button>
	</a>

	{{ include('policy_holder/_delete_form.html.twig') }}
{% endblock %}
