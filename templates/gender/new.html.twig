{% extends 'base.html.twig' %}

{% block title %}New Gender
{% endblock %}

{% block body %}
	<h1>Create new Gender</h1>

	{{ include('gender/_form.html.twig') }}

	<a href="{{ path('app_gender_index') }}">
		<button class="btn btn-secondary">back to list</button>
	</a>
{% endblock %}
